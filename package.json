{
  "name": "whitelistchecker",
  "version": "1.0.0",
  "description": "Discord bot that checks if a wallet is part of a whitelist(json file).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon ./src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@discordjs/rest": "^1.3.0",
    "discord.js": "^14.6.0",
    "dotenv": "^16.0.3",
    "fs": "^0.0.1-security"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}